<section class='page'>
  <nav class="navbar">
    <ul class='s-ul'>
      <li><p><%= link_to authenticated_root_path do %><p class='back'>&#x2039;</p><% end %></p></li>
      <li><p>Transaction</p> </li>
      <li>
        <%= button_to "LogOut", destroy_user_session_path, method: :delete, class: "btn-out" %>
      </li>
    </ul>
  </nav>
  <h2>Create new Transaction</h2>
  <%= form_with(model: [@group, @entity], local: true, html: {class: 'form'}) do |f| %>
    <div class="field">
    <%= f.text_field :name, autofocus: true, class: 'field-s',placeholder: 'Name', autocomplete: "name", required: true %>
    </div>
    <div class="field">
    <%= f.number_field :amount, autofocus: true, class: 'field-s',placeholder: 'Amount', autocomplete: "amount", required: true %>
    </div>
    <select aria-label="Default select example" name="category_id" class='field-s'>
        <option selected >--Select Category--</option>
        <% @groups.each do |g| %>
               <option value="<%= g.id %>"><%= g.name %></option>
        <% end %>
    </select>

    <div class='s-actions'>
      <div class="actions">
        <%= f.submit "Save", class: 'actions-btn' %>
      </div>
    </div>
  <% end %>
</section>